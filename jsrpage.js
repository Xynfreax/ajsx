$$.require(['../framework/all'],function(){
  $.add=
  {
    tag:'div'
    ,id:'jsrpage'
    ,children:[

    ]
    ,deterIfBoundPassed:function(){
      var wind=this;
      for (var i = 0; i < this.children.length; i++) {
        if( actChild.offsetTop+actChild.offsetHeight>wind.offsetHeight){

          if(actChild.tag==='jsrtable'){

          }
        }
      }
    }
  }
})
